import React from 'react';
import './Spinner.css'; // Make sure this CSS file contains the spinner styles

const Spinner = () => {
  return <div className="spinner"></div>;
};

export default Spinner;
